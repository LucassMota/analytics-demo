/**
 * PostCSS config for Next.js 16 using Tailwind CSS v3 and Autoprefixer
 * https://nextjs.org/docs/app/building-your-application/styling/tailwind-css
 */

/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
